#include "gamestruct.h"

/*
int main()
{
    int option;
    printf("NEW(0)or CONTINUE(1):\n");
    scanf("%d",&option);
	system("CLS");
    while(option != 0 &&option !=1)
    {
        printf("NEW(0)or CONTINUE(1):\n");
        while(getchar()!= '\n');
        scanf("%d",&option);
    };
	getchar();
    tetris(option);
	return 0;
}
*/

int main()
{
    //TestSearch();
    menu();
    return 0;
}